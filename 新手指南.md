# 新手指南（不會寫程式也能用）

這份工具會每天自動幫你找澳洲打工度假（WHV）職缺，篩選出對 WHV 簽證友善的職缺，整理成清單。

## 最快的方法：直接看今天的職缺清單

不用下載、不用安裝任何東西，點連結就能看：

- **目前所有在線職缺**：https://tortoise92113.github.io/Tryrecue/Pages/au_jobs_all.html
- 這個連結是固定的，每天都會自動更新內容，存起來、加到我的最愛就好，不用每天換連結。

如果只是想看職缺，看到這裡就可以關掉這份指南了。

以下步驟只有在你想要**自己客製化**（例如改成搜尋別的城市、別的職缺關鍵字）時才需要。

---

## 想要自己跑一份客製化版本

大概需要 30-60 分鐘，主要花在申請帳號跟金鑰，程式本身不用寫。

### 步驟 1：安裝 Python

1. 到 https://www.python.org/downloads/ 下載並安裝（選最新版即可）
2. **安裝時一定要勾選畫面下方的「Add Python to PATH」**，這是最容易漏掉、也最常出錯的一步
3. 安裝完成後，按 Windows 鍵，輸入 `cmd`，打開「命令提示字元」，輸入 `python --version`，能顯示版本號（例如 `Python 3.12.0`）就是裝好了

### 步驟 2：下載這個專案

1. 到這個網址：https://github.com/Tortoise92113/Tryrecue
2. 點右上角綠色的「Code」按鈕 → 「Download ZIP」
3. 把下載的 ZIP 檔解壓縮到你想放的資料夾（例如桌面）

### 步驟 3：安裝需要的套件

1. 打開「命令提示字元」，輸入 `cd ` 後面接你解壓縮的資料夾路徑，按 Enter（例如 `cd C:\Users\你的名字\Desktop\Tryrecue-main`）
2. 依序貼上並執行以下兩行指令：
   ```
   pip install -r whv-job-tracker/requirements.txt
   playwright install chromium
   ```
3. 這步會花幾分鐘下載東西，跑完沒有紅字錯誤訊息就是成功了

### 步驟 4：申請兩把「鑰匙」（API 金鑰）

程式要抓職缺、判斷職缺內容，各需要一把「金鑰」，都是免費申請（這個版本不會寄信，不需要 Gmail 密碼）：

| 要做什麼 | 去哪申請 | 拿到什麼 |
|---|---|---|
| 抓職缺資料 | https://developer.adzuna.com/ 註冊帳號 | `app_id` 跟 `app_key` 兩組代碼 |
| AI 判斷職缺內容 | https://aistudio.google.com/ 登入 Google 帳號、建立 API key | 一組 `api_key` |

### 步驟 5：填寫設定檔

1. 在解壓縮的資料夾裡找到 `whv-job-tracker/config.example.yml`
2. 複製一份，改名成 `config.yml`（一樣放在 `whv-job-tracker/` 資料夾裡）
3. 用記事本打開 `config.yml`，把上一步拿到的金鑰貼到對應欄位，例如：
   ```yaml
   adzuna:
     app_id: 這裡貼你的 app_id
     app_key: 這裡貼你的 app_key
   gemini:
     api_key: 這裡貼你的 api_key
   ```
4. 也可以在 `search.cities` 跟 `search.keywords` 改成你想搜尋的城市／職缺關鍵字

**`config.yml` 裡面都是你的私人金鑰，不要分享給別人、也不會被上傳到 GitHub。**

### 步驟 6：開始使用

回到解壓縮出來的最外層資料夾，直接雙擊執行：

- **`getdata.bat`** — 抓取今天的職缺並用 AI 分類（跑完視窗會自動暫停，按任意鍵關閉）
- **`start.bat`** — 開啟網頁介面，瀏覽器會自動打開 `http://localhost:5000`，可以用網頁篩選、收藏、標記職缺
- **`stop.bat`** — 關閉網頁介面

> Windows 可能會跳出「Windows 已保護您的電腦」的藍色警告視窗，這是因為 `.bat` 檔沒有付費做「發行者簽名」，不是病毒。點「其他資訊」→「仍要執行」即可。

> ⚠️ **這個網頁介面只給自己這台電腦用**，裡面刪除職缺等功能沒有密碼保護。預設只有這台電腦自己連得到，請不要用路由器連接埠轉發（port forwarding）、ngrok 之類的工具把它開放給其他裝置或網路上的人連線。

---

## 常見問題

**Q：雙擊 `.bat` 檔案沒反應／閃一下就關掉？**
代表執行時出錯了。用「命令提示字元」`cd` 到資料夾後，直接輸入檔名（例如 `getdata.bat`）執行，就能看到完整的錯誤訊息。

**Q：抓不到職缺／一直顯示錯誤？**
先確認步驟 4 的兩把金鑰都正確貼到 `config.yml` 裡，且沒有多餘的空格或缺漏的引號。

**Q：想要它每天自動跑，不用手動雙擊？**
可以用 Windows「工作排程器」設定每天固定時間自動執行 `getdata.bat`，或執行 `whv-job-tracker/run_scheduler.py`（進階用法，需要保持電腦開機）。
